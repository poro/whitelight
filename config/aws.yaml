deployment:
  mode: "aws"
  log_level: "INFO"
  log_format: "json"

secrets:
  provider: "aws_secrets_manager"
  aws_region: "us-east-1"
  aws_secret_name: "whitelight/credentials"

alerts:
  provider: "sns"

brokerages:
  alpaca:
    paper: false

  ibkr:
    gateway_port: 7496
